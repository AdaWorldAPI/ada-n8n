[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
numReplicas = 1

# n8n listens on 5678
[deploy.healthcheck]
path = "/healthz"
timeout = 300
interval = 30
